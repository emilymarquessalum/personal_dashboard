




import 'package:flutter/material.dart';
import 'package:flutter_bloc/flutter_bloc.dart';
/*

class MDISupportedScreen extends StatelessWidget {

  final Widget body;
 // final
  const MDISupportedScreen({super.key,
  required this.body,
  });

  @override
  Widget build(BuildContext context) {
    return BlocProvider<StackedViewsBloc>(
      create: (context)=> StackedViewsBloc(),
      child: Stack(
        children: [
          StackedViews(),
          body,
        ],
      ),
    );
  }
}

class StackedViews extends StatelessWidget {
  const StackedViews({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      children:
        context.read<StackedViewsBloc>().state
    );
  }
}
*/